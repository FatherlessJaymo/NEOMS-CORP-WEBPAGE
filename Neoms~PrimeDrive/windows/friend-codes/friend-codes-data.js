/* ============================================================
   FRIEND CODES — DATA
   Edit this file to add your game friend codes.

   Each entry has:
     game  — display name of the game / platform
     code  — the friend code itself (any format)
     note  — optional extra info (username, server, etc.)

   The list renders in the order you put them here. Leave the
   array empty to show the "// NO CODES YET" empty state.

   Example entry (uncomment and edit):

     { game: "Pokemon Scarlet", code: "SW-1234-5678-9012", note: "FatherlessJaymo" }
============================================================ */
var NEOMS_FRIEND_CODES = [{ game: "PokectMine 3", code: "SW-1234-5678-9012", note: "FatherlessJaymo" }];
